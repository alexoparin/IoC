namespace AMO.IoC
{
    // TODO: I expect this object to contain enough info about how specific
    // dependencies must be resolved. That means to provide values for specific arguments
    // or override resolution type or it's registration name.
    public interface IResolveOverrides
    {
    }
}